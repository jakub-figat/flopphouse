import styled from 'styled-components';

import { fontSize, colors } from '../../../config/stylesConfig';

export const PageWrapper = styled.div`
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
`;
